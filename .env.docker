# Build-time environment file for Docker
# These are dummy values used only during the build process
# Real values are injected by Coolify at runtime

NODE_ENV=production
TZ=Europe/Paris
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info
APP_KEY=docker-build-key-minimum-32-characters-required
SESSION_DRIVER=cookie
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_DATABASE=portail_cabinet
